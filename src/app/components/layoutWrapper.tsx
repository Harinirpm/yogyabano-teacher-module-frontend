// "use client"; 
// import React, { ReactNode } from "react";
// import withLayout from "./withLayout";

// type LayoutWrapperProps={
//   children: ReactNode;
// };
// const LayoutWrapper:React.FC<LayoutWrapperProps>=({ children }) => {
//   return <>{children}</>;
// };
// export default withLayout(LayoutWrapper);
